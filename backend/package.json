{
  "name": "tic-registration-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight backend and admin review app for TIC registration flow",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js"
  }
}
